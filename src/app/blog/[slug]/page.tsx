import type{Metadata}from"next";import{notFound}from"next/navigation";import{posts}from"@/data/site";import{PageHero}from"@/components/page-hero";export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=posts.find(x=>x.slug===slug);return{title:p?.title??"Guide",description:p?.summary,alternates:{canonical:`/blog/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();return <><PageHero eyebrow={p.category} title={p.title} copy={p.summary}/><article className="section"><div className="container max-w-3xl"><p className="border-l-2 border-[#b89762] pl-6 text-xl leading-9">This editorial page is prepared for a fully researched, expert-reviewed article before publication.</p><h2 className="display mt-14 text-3xl font-semibold">What this guide will cover</h2><ul className="mt-6 list-disc space-y-3 pl-6 leading-7 text-[#6b6b6b]"><li>How the material differs in everyday applications</li><li>Safety, environment, finish and maintenance considerations</li><li>Questions to ask before requesting a quotation</li></ul><p className="mt-10 text-sm leading-7 text-[#6b6b6b]">Thin or unverified AI copy has intentionally not been published. Replace this launch placeholder with original guidance reviewed by Techno Group.</p></div></article></>}
