import type{MetadataRoute}from"next";import{products,services,posts}from"@/data/site";export default function sitemap():MetadataRoute.Sitemap{const b=process.env.NEXT_PUBLIC_SITE_URL??"https://technogroup.example.com";const paths=["","/about","/products","/services","/projects","/blog","/contact","/get-a-quote",...products.map(x=>`/products/${x.slug}`),...services.map(x=>`/services/${x.slug}`),...posts.map(x=>`/blog/${x.slug}`)];return paths.map((url,i)=>({url:b+url,lastModified:new Date(),changeFrequency:i===0?"weekly":"monthly",priority:i===0?1:.7}))}
