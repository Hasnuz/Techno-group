import Link from"next/link";import{Phone,MessageCircle,FileText}from"lucide-react";
export function MobileActions(){return <aside aria-label="Quick contact" className="fixed inset-x-3 bottom-3 z-40 grid grid-cols-3 border border-black/10 bg-white/95 p-2 shadow-2xl backdrop-blur md:hidden">{[[Phone,"Call","/contact"],[MessageCircle,"WhatsApp","/contact"],[FileText,"Quote","/get-a-quote"]].map(([Icon,label,href])=><Link href={href as string} key={label as string} className="flex flex-col items-center gap-1 py-2 text-[10px] font-semibold"><Icon size={17}/>{label as string}</Link>)}</aside>}
