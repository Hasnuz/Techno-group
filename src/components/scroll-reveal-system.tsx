"use client";
import{useEffect}from"react";
export function ScrollRevealSystem(){useEffect(()=>{if(matchMedia("(prefers-reduced-motion: reduce)").matches)return;const sections=[...document.querySelectorAll<HTMLElement>(".home-page .section")];const cards=[...document.querySelectorAll<HTMLElement>(".home-page a.group")];const labels=[...document.querySelectorAll<HTMLElement>(".home-page .eyebrow")];const nodes=[...sections,...cards,...labels];nodes.forEach((node,index)=>{node.classList.add("scroll-reveal-ready");if(cards.includes(node))node.style.setProperty("--reveal-delay",`${(cards.indexOf(node)%3)*70}ms`);else if(labels.includes(node))node.style.setProperty("--reveal-delay","80ms");else node.style.setProperty("--reveal-delay",`${Math.min(index*20,120)}ms`)});const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("scroll-reveal-visible");observer.unobserve(entry.target)}})},{rootMargin:"0px 0px -10% 0px",threshold:.08});nodes.forEach(node=>observer.observe(node));return()=>observer.disconnect()},[]);return null}
